name: Site checks

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"
      - name: Test calculation helpers
        run: node --test tests/calculations.test.js
      - name: Check site structure, accessibility, and links
        run: python3 tests/site_checks.py
